# Google OAuth Setup Guide

Это руководство поможет вам настроить авторизацию через Google для приложения NextLevel Food.

## Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Войдите в свой Google аккаунт
3. Создайте новый проект или выберите существующий

## Шаг 2: Настройка OAuth consent screen

1. В боковом меню выберите **APIs & Services** → **OAuth consent screen**
2. Выберите **External** (для публичного приложения) и нажмите **Create**
3. Заполните обязательные поля:
   - **App name**: NextLevel Food
   - **User support email**: ваш email
   - **Developer contact information**: ваш email
4. Нажмите **Save and Continue**
5. На странице **Scopes** нажмите **Add or Remove Scopes**
6. Выберите следующие scopes:
   - `userinfo.email`
   - `userinfo.profile`
7. Нажмите **Update** → **Save and Continue**
8. Добавьте тестовых пользователей (если приложение в режиме Testing)
9. Нажмите **Save and Continue**

## Шаг 3: Создание OAuth 2.0 credentials

1. В боковом меню выберите **APIs & Services** → **Credentials**
2. Нажмите **Create Credentials** → **OAuth client ID**
3. Выберите **Application type**: **Web application**
4. Введите **Name**: NextLevel Food Web Client
5. В разделе **Authorized JavaScript origins** добавьте:
   - `http://localhost:3000` (для разработки)
   - `https://food.does.cool` (для продакшена)
6. В разделе **Authorized redirect URIs** добавьте **ТОЧНО** как указано:
   
   **Для разработки:**
   ```
   http://localhost:3000/api/auth/callback/google
   ```
   
   **Для продакшена:**
   ```
   https://food.does.cool/api/auth/callback/google
   ```
   
   **⚠️ ВАЖНО - Частые ошибки:**
   - ❌ `http://localhost:3000/api/auth/callback/google/` (лишний слеш в конце)
   - ❌ `http://127.0.0.1:3000/api/auth/callback/google` (использован IP вместо localhost)
   - ❌ `http://localhost:3000/api/auth/google/callback` (неправильный порядок)
   - ✅ `http://localhost:3000/api/auth/callback/google` (ПРАВИЛЬНО!)

7. Нажмите **Create**
8. Скопируйте **Client ID** и **Client Secret**

## Шаг 4: Настройка переменных окружения

1. Создайте файл `.env.local` в корне проекта
2. Добавьте следующие переменные:

```env
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
```

3. Генерируйте `NEXTAUTH_SECRET`:
   - В Git Bash или WSL выполните: `openssl rand -base64 32`
   - В PowerShell выполните: 
     ```powershell
     [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
     ```
   - Или используйте онлайн генератор: https://generate-secret.vercel.app/32

4. Замените `your-client-id` и `your-client-secret` на значения из Google Cloud Console

## Шаг 5: Запуск приложения

1. Установите зависимости (если еще не установлены):
   ```bash
   pnpm install
   ```

2. Запустите сервер разработки:
   ```bash
   pnpm dev
   ```

3. Откройте браузер по адресу `http://localhost:3000`

4. Нажмите **Sign in** в header и авторизуйтесь через Google

## Шаг 6: Проверка работы

1. После успешной авторизации в header должно отображаться ваше имя
2. Перейдите на страницу создания рецепта `/meals/share`
3. Поля **Your name** и **Your email** должны автоматически заполниться данными из Google аккаунта
4. Поле **Your email** будет заблокировано для редактирования (это привязка рецепта к вашему аккаунту)

## Продакшен настройки

Для развертывания на сервере:

1. Обновите переменные окружения на сервере:
   ```env
   NEXTAUTH_URL=https://food.does.cool
   NEXTAUTH_SECRET=your-production-secret
   GOOGLE_CLIENT_ID=your-client-id
   GOOGLE_CLIENT_SECRET=your-client-secret
   ```

2. Убедитесь, что в Google Cloud Console добавлены продакшен redirect URIs

3. Если приложение в режиме **Testing**, переключите его в режим **Production** в OAuth consent screen

## Возможные проблемы и решения

### ❌ Ошибка "Error 400: redirect_uri_mismatch"

**Симптомы**: Google показывает страницу с ошибкой "redirect_uri_mismatch"

**Причина**: Redirect URI в Google Cloud Console не совпадает с тем, что отправляет приложение

**Решение**:

1. **Проверьте redirect URI в Google Console:**
   - Откройте [Google Cloud Console](https://console.cloud.google.com/)
   - **APIs & Services** → **Credentials**
   - Нажмите на ваш OAuth 2.0 Client ID
   - В **Authorized redirect URIs** должно быть **ТОЧНО**:
     ```
     http://localhost:3000/api/auth/callback/google
     ```

2. **Проверьте `.env.local`:**
   ```env
   NEXTAUTH_URL=http://localhost:3000
   ```
   
   **Важно**: НЕ используйте `127.0.0.1`, только `localhost`!

3. **Частые ошибки:**
   - ❌ Лишний слеш: `http://localhost:3000/api/auth/callback/google/`
   - ❌ IP адрес: `http://127.0.0.1:3000/api/auth/callback/google`
   - ❌ Неправильный протокол: `https://localhost:3000/...`
   - ✅ ПРАВИЛЬНО: `http://localhost:3000/api/auth/callback/google`

4. **После изменений:**
   - Сохраните в Google Console
   - Подождите 5-10 минут (изменения применяются не мгновенно)
   - Очистите cookies или используйте режим инкогнито
   - Перезапустите dev сервер: `pnpm dev`

### ❌ Ошибка "Access blocked: This app's request is invalid"

**Решение**:
- Проверьте, что OAuth consent screen настроен правильно
- Убедитесь, что добавлены необходимые scopes (userinfo.email, userinfo.profile)
- Если в Testing mode, добавьте свой email в Test users

### ❌ Не отображается имя пользователя после входа

**Решение**:
- Проверьте консоль браузера на ошибки
- Убедитесь, что сессия сохраняется корректно
- Проверьте что `NEXTAUTH_SECRET` установлен в `.env.local`

### ❌ Email не заполняется автоматически

**Решение**:
- Очистите кеш браузера
- Проверьте, что endpoint `/api/auth/session` возвращает данные пользователя
- Откройте DevTools → Network → проверьте запрос к `/api/auth/session`

### 💡 Отладка

Чтобы увидеть больше информации об ошибках NextAuth:

1. Добавьте в `.env.local`:
   ```env
   NEXTAUTH_DEBUG=true
   ```

2. Перезапустите сервер и проверьте логи в терминале

### Ошибка "Access blocked: This app's request is invalid"
- Проверьте, что OAuth consent screen настроен правильно
- Убедитесь, что добавлены необходимые scopes

### Не отображается имя пользователя после входа
- Проверьте консоль браузера на ошибки
- Убедитесь, что сессия сохраняется корректно

### Email не заполняется автоматически
- Очистите кеш браузера
- Проверьте, что endpoint `/api/auth/session` возвращает данные пользователя

